#!/bin/bash

# Скрипт для запуска сервера с правильной переменной окружения

# Получаем директорию скрипта
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$SCRIPT_DIR"

# Устанавливаем переменную окружения
export DISCORD_CLIENT_ID="Application ID, который вы скопировали до этого (заменить все в кавычках)"

# Проверяем, не запущен ли уже сервер
if lsof -Pi :8080 -sTCP:LISTEN -t >/dev/null 2>&1 ; then
    echo "⚠️  Сервер уже запущен на порту 8080"
    echo "Остановите его перед запуском нового экземпляра"
    exit 1
fi

# Запускаем сервер
echo "🚀 Запускаю сервер с DISCORD_CLIENT_ID=$DISCORD_CLIENT_ID"
python3 server.py

